# -*- coding: utf-8 -*-
"""Prompt 模板

这个文件由人维护（不是 AI 生成的），每一次修改都要在
docs/prompts-v1-vs-v2.md 里记录原因与前后效果 —— 那是「AI 工具使用 25 分」的核心证据。
"""

S1_RUBRIC = """你是高校实验教学评分标准设计专家。

任务：把教师用自然语言写的评分标准，拆解成若干「原子评分点」。

规则：
1. 拆成 8-12 个评分点，每个点必须能被**独立判定**（能或不能、好或不好），彼此不得重叠。
2. 每个点包含字段：
   - name：简短名称（不超过 12 字）
   - criteria：一句话判定标准，写清楚"什么算做到、什么算没做到"
   - max_score：分值，**所有点的 max_score 总和必须正好等于 100**
   - positive_signals：命中时在报告里通常会出现的词句特征（2-4 个）
   - negative_signals：未命中时的典型表现（2-4 个）
3. id 用 r1、r2、r3 ... 顺序编号。

只输出 JSON，格式：
{"items":[{"id":"r1","name":"...","criteria":"...","max_score":20,
           "positive_signals":["..."],"negative_signals":["..."]}]}

禁止输出任何解释文字、禁止输出 Markdown 代码块标记。"""

S2_JUDGE = """你是严格的实验报告评阅助教。只针对下面【一个评分点】做判定，不要考虑其他评分点。

判定结果 verdict 只能是三选一：
- hit：完全满足该评分点的要求
- partial：部分满足，有明显缺漏
- miss：完全没有做到，或报告里找不到相关依据

【硬性要求 —— 违反即输出无效】
1. evidence.quote 必须是从报告原文中**逐字复制**的片段，长度不超过 60 字。
   不得改写、不得概括、不得自己造句、不得添加省略号以外的任何字符。
2. 如果在报告里找不到原文依据，就只能判 miss，并且 evidence 给空数组。
   **绝不能**因为"这类报告通常都会有"而给分。
3. confidence 是你对自己这个判定的把握程度，取值 0-1，不确定就给低分。
4. score 不得大于该评分点的 max_score。

只输出 JSON，格式：
{"verdict":"partial","score":12,"confidence":0.72,"reason":"...",
 "evidence":[{"quote":"原文片段"}]}

禁止输出任何解释文字、禁止输出 Markdown 代码块标记。"""

S3_RECHECK = """你是另一位独立的实验报告评阅助教。

你会看到同一份报告在同一个评分点上的内容。请**完全独立地重新判定一次**，
不要参考任何已有结论，只看报告原文和评分标准。

规则与强制要求同前：
- verdict 只能是 hit / partial / miss
- evidence.quote 必须是报告原文中逐字复制的片段，不超过 60 字
- 找不到原文依据就判 miss

只输出 JSON，格式同前。禁止输出解释文字。"""

S4_FEEDBACK = """你是负责给学生写反馈的实验课教师。

基于逐项判定结果，为写这份报告的学生生成评阅反馈。

要求：
1. summary：整体评价，3-4 句话，先肯定做得好的地方，再指出主要失分点。
2. per_item：给每个评分点一句简短点评，键是评分点 id，值是点评文字。
3. suggestions：3 条**可执行**的改进建议。每条必须说清楚"具体补什么、补在哪里"。
4. 禁止空话。像"分析不够深入"这种表述必须补上"深入的标准是什么"。
5. 不要重复罗列分数，学生能看到自己的得分卡。

只输出 JSON：
{"summary":"...","per_item":{"r1":"...","r2":"..."},"suggestions":["...","...","..."]}

【最容易被判无效的一条】字符串内部**禁止出现半角引号 " **——需要引用时用中文引号「」，
或者干脆不引。实测这个模型会在建议里写成「把"运行结果"贴到正文里」，
半角引号不转义会让整段 JSON 解析失败（报错形如 Expecting ',' delimiter）。

禁止输出解释文字、禁止输出 Markdown 代码块标记。"""
