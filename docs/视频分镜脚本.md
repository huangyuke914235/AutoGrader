# 3 分钟演示视频分镜脚本（含口播逐字稿）

> 用法：左边是时间轴，中间是**画面上要发生的操作**，右边是**照着念的口播**。
> 彩排 2 遍再录；录屏建议 1920×1080、浏览器全屏、关掉所有通知与弹窗。
>
> **三条不可违反的录制纪律**（对应我们的防自欺规则）：
> 1. 必须跑**真实模型、真实报告**，不允许预置假结果、不允许剪接成"秒出分"的假象；
>    等待过程可以加速，但要在字幕上写"此处已加速 N 倍"。
> 2. 演示用的报告必须是**已脱敏**的版本，学号/姓名不能出现在画面里。
> 3. 视频里说的每一个数字，都必须和仓库里的 `tools/benchmark.py` 输出一致；
>    若录制前重跑过 benchmark，**以新的数字为准**。

---

## 分镜表

| 时间 | 画面 / 操作 | 口播逐字稿 | 字幕 / 标注 |
|---|---|---|---|
| **0:00–0:12** | 黑底白字打出两行：助教批改 60 份实验报告 ≈ 10 小时；同一份报告连跑 10 次，2 次判定会漂 | 「一个助教批改 60 份实验报告，要花掉差不多 10 个小时；更麻烦的是，同一份报告、同一套标准，过一会儿再改，分数可能就不一样了。」 | 字幕：10 小时 / 2 次漂移（数据来自 stability_log.json） |
| **0:12–0:22** | 切到 PPT 第 2 页（痛点），停 3 秒 | 「所以我们没有做"让 AI 打个分"，因为那只是把黑箱换了个更大的黑箱。我们做的是——把老师的评分标准，变成一条**可核查、可溯源、可校准**的判定流水线。」 | 「AutoGrader · 实验报告智能评阅平台」 |
| **0:22–0:45** | 打开在线 Demo。①「内置脱敏样本」选 `S02`，显示字数与章节数；②在右侧输入自然语言评分标准 | 「先看真实操作。左边是一份已经脱敏的实验报告，右边是老师用大白话写的评分标准——就一句话，比如'有实验目的、步骤完整、有运行结果、有分析总结，总分 100'。」 | 高亮：报告 5,677 字 / 22 章节 |
| **0:45–1:05** | 点「① 生成评分点」→ 出现 N 个原子评分点 → 切到「③ 评分点」页，改一个分值，把合计展示为 100 | 「第一步是把它拆成可以单独判定的原子评分点。注意这里有个**人在回路**的关口：老师可以改分值、可以增删，改完立刻生效。系统不会替老师把标准定死。」 | 标注「人在回路 · 关口一」 |
| **1:05–1:35** | 回到「① 评阅」→ 点「② 开始评阅」→ 进度条逐点推进（此处可加速，标注加速倍数） | 「第二步是逐个评分点判定。模型每判一个点，都必须回引报告原文——不是它自己说'有'，而是必须在正文里找到那句话。」 | 字幕：逐点判定中（此处已加速 3×） |
| **1:35–2:05** | 出分后切到「② 详情对照」：左侧原文高亮，右侧逐点卡片展示判定/置信度/理由/证据 | 「出分之后是最关键的一页：左边是报告原文，命中的证据已经高亮；右边是每个评分点的判定、置信度和理由。任何一个分数，都能追到原文里那一行字。」 | 圈出「置信度 0.85」「待复核」标记 |
| **2:05–2:25** | 点某条证据的「定位」按钮 → 左侧对应句子变深黄 → 在「人工覆盖」里把某个分数改掉并填理由 | 「点'定位'，原文直接跳到那句话。如果老师不认同，可以直接改分并留下理由——**最终拍板的永远是人**，AI 只负责给出带依据的初判。」 | 标注「人在回路 · 关口二：可覆盖、留痕」 |
| **2:25–2:50** | 切到「⑤ 批量测试」：展示多份报告的对比表 / 与人工分的差值 | 「我们还给自己做了一次闭卷考试：10 份报告由不写代码的队友独立人工打分、封存，直到评测当天才拆。第一次跑出来是 31.67 分——那次我们查出模型只看到了报告的 7%。修好之后完整重跑是 20.44 分；再修掉 PDF 断行和'一条短引用否掉整条判定'这两个缺陷，深度整改后跑完整流水线，最终是 <strong>6.89 分</strong>，误差 5 分以内占 44.4%，引用可溯源率 97%，相关系数 0.936。<strong>但这个修复发生在 gold 解封之后，我们没法自证它没沾到 gold 的光，所以主页上几个数字都挂着，由评委自己判断。</strong>」<br>补一句短板：「还有个没解决的问题：9 份里 5 份被评到 100 分，高分区已经失去区分度——这是我们下一步要修的。」 | 三个数字依次放大出现：「31.67 → 20.44 → 6.89」用箭头串起来，最后标注「修复后，已披露」，再闪一下「5/9 满分 = 天花板效应（未解决）」 |
| **2:45–2:58** | 切到 PPT 第 3 页：RAG-E 四阶链 + 三条铁律 | 「整条链路叫 RAG-E：评分点原子化、证据锚定判定、一致性守卫、反馈生成。三条铁律是——分数由代码加总、无证据的判断不算数、不确定就交给人。模型唯一能输出的，是单个点的命中与否；总分它碰不到。」 | 「R → A → G → E」逐段点亮 |
| **2:58–3:00** | 团队页 / 仓库二维码 | 「我们是深圳大学的两名本科生。代码、排查记录和这份自评测，全部开源在仓库里，欢迎复现。」 | 仓库地址 + 二维码 |

---

## 录制前检查清单

- [ ] 在线 Demo 已成功重建（推送最新代码后等 Streamlit Cloud 显示 Running）
- [ ] 准备 3 份脱敏样本（云端只有 S02/S03/S04），本地可多备几份
- [ ] 浏览器全屏 1920×1080，隐藏书签栏，关闭微信/邮件通知
- [ ] 录屏前先在浏览器里完整跑一遍，确认不会中途断网或超时
- [ ] 口播稿打印或放提词器，控制在 3 分钟以内（超时会被判不合格就按赛方要求裁剪）
- [ ] 导出 mp4（H.264），文件名 `AutoGrader-3分钟演示.mp4`
- [ ] **视频里的数字与最新 benchmark 输出一致**；不一致就按新数字重录这几句
- [ ] 上传后回填主页 `docs/index.html` 里的 `VIDEO_URL`

## 常见翻车点

| 翻车 | 预防 |
|---|---|
| 录到一半模型超时/限流 | 录制前先跑一次预热；若失败，等待后重跑该段，不要剪接成"一次成功" |
| 报告里出现真实姓名学号 | 只用 `data/samples/` 里已脱敏的样本，录前全文扫一眼 |
| 说错数字 | 数字单独录一条备用素材，方便替换 |
| 页面加载慢显得"卡" | 允许加速，但字幕必须写明"已加速"，不许假装实时 |
