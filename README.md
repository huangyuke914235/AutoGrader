# AutoGrader · 实验报告智能评阅平台

> 粤港澳大湾区 AI Coding 创新大赛 · 方向一 AI + 教学管理助手
>
> **不做「让 AI 打个分」，而做「把评分标准变成可核查、可溯源、可校准的判定流水线」。**
> 分数是结果，每一个分数背后那句"证据在第 3 节，原文是……"才是产品价值。

## 团队

深圳大学计算机与软件学院 · 计算机科学与技术 · 2025 级 05 班，2 人：

- **黄宇科**（主程）：代码 / 部署 / 版本管理 / 自评测工具
- **张镒川**（产品与材料）：报告脱敏 / 评分点整理 / **独立人工打分（gold set）** / 试用 / PPT / 视频

> 评测口径：基准答案（gold set）由**不参与代码与 prompt 开发**的成员独立打分——
> 打分期间不看系统输出、不与主程讨论，完成后封存，直到评测当天才解封。
> 让写 prompt 的人同时定义「正确答案」，等于对着答案改答案。

## 核心方法论：RAG-E 四阶判定链

```
R 评分点原子化   教师用自然语言写评分标准 → 拆成可独立判定的原子评分点
A 证据锚定判定   逐评分点独立判定，必须回引报告原文；引用经代码逐字校验
G 一致性守卫     低置信自动二次复核，不一致 / 分值差距过大 / 调用失败一律转人工
E 反馈生成       基于已定稿的逐项判定生成总评与可执行改进建议
```

## 三条工程约束

1. **分数由代码加总** —— 模型只输出单点评定（hit / partial / miss 与该项分值），
   总分由代码求和；判定与分值明显矛盾时标记转人工，不会静默改分
2. **无证据的判定不算数** —— 每条引用都必须通过代码的原文**逐字匹配**校验，
   匹配不上就作废重跑（不接受模糊匹配或近似匹配）；两次都不通过则清空证据、转人工复核
3. **不确定就交给人** —— 低置信、两次判定不一致、两次分值差距过大、模型调用失败，
   全部显式标记为「待人工复核」，并在界面上单独计数

## 自评测结果（9 份真实实验报告 vs 封存的人工打分）

评测方法：**9 份**真实报告由**不参与开发的成员**独立人工打分并封存，
直到评测当天解封；系统侧走与产品完全相同的流水线。

> 另收集了 1 份篇幅超长的报告（6.7 万字），未纳入自动评测：
> 每份报告需按全文判定，超长会显著拉长单次耗时，其人工分仍保留在基准集里，不计入下列任何指标。

| 评测版本 | MAE | 误差 ≤5 分占比 | 证据可溯源率 |
|---|---|---|---|
| 修复前（保守值，作为对外主指标） | **20.44** | 0% | 84.9% |
| 迭代后 · 第 1 次（含一致性复核） | 6.89 | 44.4% | 97.0% |
| 迭代后 · 第 2 次（同代码重跑） | 6.67 | 33.3% | 96.9% |

后两次是同一份代码相隔一天跑的完整评测，**指标落在 6.67 ~ 6.89 的区间内**，
相关系数 0.907 ~ 0.936，系统失败率 0%。我们把区间写出来而不挑最好的一次——
**这个波动正是「AI 只出初评、教师终裁」的现实理由**。

已知局限（未解决，如实列出）：

- **天花板效应**：高分报告容易被评到满分，9 份中有 4~5 份得 100 分，高分区区分度不足；
- 不支持图片与截图内容（无 OCR），扫描件会显著偏低；
- 评语生成环节偶发输出格式失败（首次评测 9 份中 2 份），失败时自动退回规则兜底评语，分数不受影响。

全部数字可由 `tools/batch_run.py` + `tools/benchmark.py` 复现。

> **项目定位**：这是竞赛原型 / 教师辅助初评工具，**不是**可以无人监督直接发布正式成绩的自动评分系统。
> 系统只产出带证据的初评，最终成绩由教师确认；不支持图片与截图内容（无 OCR）。

## 快速开始（干净环境从零安装）

```bash
python -m venv .venv
source .venv/Scripts/activate        # Windows Git Bash；PowerShell 用 .venv\Scripts\Activate.ps1
pip install -r requirements.txt
pip install -r requirements-dev.txt  # 仅跑测试时需要
cp .env.example .env                 # 然后填入自己的 key（.env 不提交）
python tools/test_key.py             # 验证密钥与结构化输出是否可用
python -m pytest tests/ -q           # 无 key 也能跑，全部通过才算干净
streamlit run app.py                 # 打开 http://localhost:8501
```

`.env` 内容（也可直接复制 `.env.example`）：

```
LLM_API_KEY=sk-xxxx
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
DEMO_MODE=false
```

`DEMO_MODE=true` 时**真的不会**调用模型（代码层拦截，不只是界面提示）；
需要离线演示请先 `python tools/make_demo.py` 生成演示结果。

## 自动化测试

```bash
python -m pytest tests/ -q
```

- 78 项，全部使用 mock，**不需要 API key、不联网**。
- 覆盖：数据契约与 rubric 校验、证据校验与降级分支、一致性守卫（低置信/复核失败/不一致/分差）、
  人工改分与导出口径、解析器偏移与解析体检、隐私与 CSV 注入、提示词注入检测。
- 依赖真实模型的评测请跑 `tools/batch_run.py` + `tools/benchmark.py`（会调用模型，结果版本化保存、不覆盖历史）。

## 目录结构

```
├── app.py            # Streamlit 界面主入口（五个标签页的骨架）
├── batch_ui.py       # 第 ⑤ 个标签页「批量测试」（批量评阅 / 一致性重复测试）
├── models.py         # Pydantic 数据契约
├── prompts.py        # RAG-E 四阶段 Prompt 模板
├── llm.py            # 模型调用 + JSON 宽容解析 + 防幻觉闸门
├── parser.py         # 报告解析与章节切分（纯确定性代码，不调模型）
├── pipeline.py       # 四阶流水线编排
├── tests/            # pytest 测试（mock，无 key 可跑）
├── docs/             # GitHub Pages 作品主页 + 离线可看案例 + 三篇故障排查记录
└── tools/            # 脱敏 / 连通性自检 / 稳定性测试 / gold 打分表 / 批量评测
```

## 批量测试（界面第 ⑤ 个标签页）

两种批量能力，都可以导出 CSV 留痕：

- **多份报告批量评阅**：一次跑完选中的报告，出横向对比表；
  本地存在封存的人工 `gold.json` 时，自动对齐算出 MAE / 误差≤5 分占比 / 证据可溯源率。
- **同一份报告重复评阅**：连跑 N 次，看总分极差与评分点判定稳定率 ——
  这是「G 一致性守卫」存在的实证，稳不稳用数据说话。

> 注：自定义评分点与人工 gold 的维度对不上，此时界面会**拒绝计算 MAE**，
> 因为维度不一致时算出来的误差没有意义。

## 故障排查记录

产品迭代过程中修掉的三个真实缺陷，都保留了完整的定位与验证过程：

- [上下文丢失](docs/bugfix-上下文丢失.md) —— 模型实际只看到报告 7%~11% 的内容，导致系统性低估
- [反馈生成失败](docs/bugfix-反馈生成失败.md) —— 异常被 `except` 吞掉，线上查不到原因
- [引用匹配与 PDF 断行](docs/bugfix-引用匹配与PDF断行.md) —— 引用对不上原文，评分点被误判为 0 分

## 隐私与数据说明

- 本仓库**不包含**任何原始实验报告。`data/` 目录已在 `.gitignore` 中，原始文件只保留在本地。
- 主页 `docs/cases/` 中的演示案例做了双重处理：**全文脱敏**（学号 / 姓名 / 班级 / 电话 / 邮箱均替换为占位符并做残留扫描）
  + **裁剪为证据片段窗口**（每条证据前后各保留 500 字），不含完整作业原文。
- 脱敏脚本本身**不包含任何真实姓名与本机路径**：样本清单与已知姓名从本机配置文件读取，
  该配置已在 `.gitignore` 中（仓库内只提供模板）。
- 上传的报告写入受控临时目录（UUID 文件名，不参与任何路径拼接），解析结束即在 `finally` 中删除；
  结果文件名使用随机 ID，上传文件不会长期留存。

## 第三方开源库

| 库 | 用途 |
|---|---|
| [Streamlit](https://streamlit.io) | Web 界面（Apache-2.0） |
| [PyMuPDF](https://pymupdf.readthedocs.io) | PDF 文本提取（AGPL） |
| [python-docx](https://python-docx.readthedocs.io) | Word 文档解析（MIT） |
| [OpenAI Python SDK](https://github.com/openai/openai-python) | 调用 OpenAI 兼容接口（本作品接 DeepSeek）（Apache-2.0） |
| [Pydantic](https://docs.pydantic.dev) | 数据校验与 JSON Schema 约束（MIT） |
| [pandas](https://pandas.pydata.org) | 结果表格与 CSV 导出（BSD） |

## 许可证

[MIT](LICENSE)
